const httpConfig = {
    host:'localhost',
    port:80
}

const mysqlConfig = {
    connectionLimit:10,
    host:'xxxxx',
    port:xxx,
    user:'xxx',
    password:'xxx',
    database:'xxx'
}

module.exports = {
    httpConfig,mysqlConfig
}